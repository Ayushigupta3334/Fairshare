# AI Logs

> **Action needed before submitting:** this file must contain the *complete, unedited* conversation you had with the AI tool while building this project — pasted as-is. I (the AI) can't export our chat transcript automatically from this side, so please copy it yourself using the steps below, then replace this note with the real transcript.

## How to get the full transcript

**On claude.ai (web or desktop app):**
1. Open this conversation.
2. Select all the messages (click at the very start of the conversation, scroll to the end, shift-click at the end — or use `Ctrl/Cmd+A` after clicking into the message area).
3. Copy (`Ctrl/Cmd+C`) and paste directly into this file, replacing this note.
4. Keep both your prompts and my full replies, in order, exactly as they appeared — don't trim, summarise, or reformat them.

**If you used a different tool (Copilot, ChatGPT, etc.):**
Most have a "copy conversation" / "export" or "share" option in the conversation menu — use that rather than retyping from memory, so the log is exact.

## What to include

- Every prompt you sent, in the order you sent them, including the round-1 (browser/localStorage) version and this round-2 (Flask/SQLite) rebuild if they happened in the same conversation.
- Every full response, including any explanations, code, or file contents given inline.

## Why this matters

The instructions are explicit that this log must be pasted as-is and not modified — altering it costs marks. So resist the urge to clean it up, remove a false start, or shorten a long explanation. An honest, slightly messy log that shows real back-and-forth (including any dead ends or corrections, like the settlement test that needed fixing) is exactly what's being evaluated — it's evidence of *how* the solution was reached, not just the final result.

---

*(Replace everything above this line with the actual pasted transcript before pushing to GitHub.)*
